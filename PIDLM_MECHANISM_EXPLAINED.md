# التوثيق الفني المعماري للمرنان — الإصدار الثامن (V8 Partonic Resonance)

---

## الفلسفة الأساسية: اللغة كحقل ديناميكي مقيد بمنطق كمومي

المعنى في المرنان لا يتم حفظه كأرقام صماء، بل ينشأ من **مزامنة الأطوار وميكانيكا الكم**.
الاستدلال ينشأ من **ملاحة الجواذب**، والإعراب يُفرض كمسار طوري، والاتساق يُضمن عبر **توتر الطور وبوابة الرنين والجاذبية الدلالية**. 
**لا يوجد هنا (Backpropagation) ولا توجد شبكات عصبية.** كل كلمة هي جسيم فيزيائي له تردد، كتلة، وطور.

---

## الابتكار 1: التراكب الكمومي (Quantum Superposition)

استبدال خوارزمية `Beam Search` التقليدية بمفهوم التراكب الكمومي المستوحى من فيزياء الكم.

**الآلية:**
- الكلمات المتنافسة لا تُختار فوراً بناءً على احتمالية بسيطة، بل تُحفظ في "حالة تراكب" (Superposition State) مستمرة.
- يُقيّم النظام الانتروبيا للحالة `S_cur`. إذا كانت الانتروبيا عالية (تشوش أو شك كبير)، يستمر النظام في التفرع الكمومي.
- بمجرد انخفاض الانتروبيا دون حد حرج `S_crit`، تنهار الدالة الموجية (Wave Collapse) ويتم اختيار المسار الأفضل بشكل قاطع لتوليد الجملة.

---

## الابتكار 2: بوابة الرنين لحل الانحراف الدلالي (Resonance Gate)

في الإصدارات السابقة، كان النظام يعاني من "سلطة الكلمات" (Word Salad) بسبب هيمنة الكلمات المتكررة جداً (مثل "في") والتي تجذب أي كلمة بعدها عشوائياً. تم حل هذا عبر **بوابة الرنين**.

**الآلية:**
- البوابة لا تنظر فقط للكلمة السابقة مباشرة، بل تقيس الرنين مع **كل السياق الجملي (Context)** بحد أقصى 5 كلمات سابقة.
- بدلاً من الجمع الخطي العادي، تطبق البوابة تضخيماً غير خطي `(hits ** 1.5)` للكلمات المرشحة التي تتردد (ترتبط) مع **أكثر من كلمة واحدة** في السياق.
- هذا يمنع الكلمات الشائعة من الهيمنة، ويرغم المولد على الالتزام الصارم بالمعنى العام للجملة بدلاً من الانزلاق في استطرادات عشوائية.

---

## الابتكار 3: المستخلص المورفولوجي الديناميكي (Dynamic Root Extractor)

في السابق، كان النظام يعتمد على قائمة ثابتة (150 جذر فقط)، مما يعني فقدان الكلمات الجديدة لأوزانها الصرفية والنحوية.

**الآلية:**
- يعتمد الإصدار الخامس على `ArabicRootExtractor` وهو محلل خوارزمي ديناميكي يقوم بتجريد السوابق واللواحق والضمائر.
- يقارن الكلمة بأوزان الصرف الأساسية (مثل افْتَعَلَ، مُسْتَفْعِل) لاشتقاق الجذر الصافي (مثلاً يستخرجون -> خرج) بثقة رياضية.
- يتم إرسال هذا الجذر فوراً لـ `LetterDB` لحساب متجه طوري (Phase Vector) يمثل هذا الجذر فلسفياً، ليتم تطبيقه فوراً في الفضاء النحوي والمورفولوجي.
- **النتيجة:** النظام الآن يدعم جذوراً لا نهائية وقادر على استيعاب أي مصطلحات متخصصة.

---

## الابتكار 4: RAM — الذاكرة الزمنية المتلاشية (Temporal Decay Planning)

بدلاً من حفظ الكلمات كتوكنات، نحفظ حالات الاستقرار الطوري `A_k = {φ_center, σ, age, words}` لكل المخرجات السابقة.

**الآلية:**
- `observe(words)`: يحسب متوسط متجهات الكلمات ← φ_center، ويهيئ عمر الجاذب.
- `tick()`: تزيد أعمار الجواذب بمرور الجمل لضمان تضاؤل تأثير الماضي السحيق `exp(-decay * age)`.
- `retrieve_context(φ)`: يسترجع كلمات من أقرب جاذب متبقٍ كسياق إضافي للمولد للمحافظة على تماسك القصة أو الحوار الطويل.

---

## الابتكار 5: الدلالة الفلسفية للحروف (16D Philosophical Semantics)

الفضاء الرياضي للمرنان يتكون من 64 بُعداً هندسياً:
- **22 بُعد طوري** (فيزيائي).
- **8 أبعاد جذرية** (ROOT_DIMS).
- **6 أبعاد DDE** (تمدد الأبعاد الديناميكي والانتروبيا).
- **6 أبعاد للنحو** (مرتكزات غير متعامدة Kuramoto mean-cos).
- **16 بُعد للدلالة الفلسفية.**
- **6 أبعاد للقصد** (Pragmatics).

كل حرف عربي تم تشفيره بمعناه الفلسفي الأصلي (مثال: "ب" للبناء، "خ" للخفاء). يتم تحويل الكلمات إلى موجات جيبية (Sine/Cosine Phase Vector) تدمج فلسفة كل حرف بحسب وزنه في الكلمة.

---

## الابتكار 7: الفضاء الطوري ثنائي اللغة (Bilingual Phase Resonance)

النظام لم يعد مقتصراً على اللغة العربية فحسب، بل تم توسيعه ليفهم اللغة الإنجليزية ولغات البرمجة باستخدام نفس المبادئ الفيزيائية (بدون تدريب شبكات عصبية).

**الآلية:**
- **UniversalRootExtractor:** محلل مورفولوجي عالمي يميز تلقائياً بين الكلمات العربية والإنجليزية. يقوم بتجريد الكلمات الإنجليزية من اللواحق (مثل `ing`, `ed`, `ly`) ويعالج الأفعال الشاذة (مثل `thought` -> `think`).
- **EnglishCharacterEmbedding:** تماماً كالحروف العربية، تم منح الحروف الإنجليزية الـ 26 قيماً فيزيائية فلسفية مبنية على "اللفظ والشكل" (مثال: **A** = المركزية والانطلاق، **S** = الجريان والتموج، **C** = الاحتواء والانحناء).
- **الجاذبية البرمجية:** يستطيع النظام قراءة شفرات برمجية كاملة (مثل خوارزميات بايثون) وتحويل الكلمات المحجوزة (`def`, `async`, `class`) إلى جواذب طورية تتفاعل وتتجاذب مع بعضها في فضاء المعنى.
- **الصمود الدلالي (Semantic Robustness):** الكلمات المعقدة جداً أو المصطلحات المتخصصة مثل `polymorphism` تمر كمتجهات طورية سليمة تعتمد على ترددات حروفها، مما يجعل النظام عصياً على الانهيار الدلالي.

---

## الابتكار 6: الرنين الصرفي (Morphological Weight Resonance)

في الإصدارات السابقة، كان النظام يتعامل مع الصرف كمتجه نحوي فقط دون ربط بين الأوزان الصرفية المختلفة.

**الآلية:**
- `WeightResonanceEngine` في `weight_resonance.py` يحتوي على 14 وزناً صرفياً عربياً أساسياً (فَعَلَ، فَعَّلَ، فَاعَلَ، أَفْعَلَ، تَفَعَّلَ، تَفَاعَلَ، اِنْفَعَلَ، اِفْتَعَلَ، اِفْعَلَّ، مَفْعُول، فَعِيل، فَعَّال، فُعُول، أَفْعَال) مع متجهات طورية 22D.
- لكل وزن متجه طوري فريد يمثل "بصمته الفيزيائية" في فضاء المعنى.
- **جداول انتقالية (Transition Tables):** تحدد احتمالية الانتقال من وزن إلى آخر في سياق سليم (مثل فَعَلَ ← مَفْعُول بثقة 0.9، فَعَّلَ ← تَفَعَّلَ بثقة 0.8).
- يتم دمج score الرنين الصرفي في معادلة `_score()` بوزن 1.20.

## الابتكار 7: التقرير الفيزيائي (Physics Report)

كل توليدة الآن تصاحبها بيانات فيزيائية كاملة:
- **word_masses:** كتلة كل كلمة (m = E/c²)
- **phase_angles:** زاوية طور كل كلمة في الفضاء 64D (0.0 — 1.0)
- **alignments:** درجة محاذاة كل كلمة للهدف الطوري (متوافق/منحرف/متضاد)
- **morph_weights:** توزيع الأوزان الصرفية في النص المولد
- **ram_size:** عدد الجواذب المخزنة في الذاكرة
- **vocab_size:** حجم المفردات في المعجم

## الابتكار 8: التغذية المعرفية التلقائية (Auto-Corpus Scaling)

النظام لم يعد مقيداً بملف نصي واحد. 
**الآلية:**
- يقوم خادم `FastAPI` بفحص مجلد `data/` بالكامل.
- يقوم برصد وقراءة كافة الملفات النصية (`.txt`) في المجلد سواء كانت مقالات طبية، أشعار، أو روايات.
- يدمجها جميعاً في **مصفوفة الاقتران الفيزيائية (K Matrix)** لحظة بدء التشغيل.

---

## الابتكار 9: Spectral Wave Engine (V5.3)

استبدال المتجهات الطورية الثابتة بتمثيل الكلمة كحزمة موجية (wave packet) في مجال التردد:

**الآلية:**
- `compute_word_wave(word)` → FFT لمتجه الحروف الطوري ← `{freqs, magnitudes, phases, ω₀, A₀, φ₀}`
- Sidebands من co-occurrence: لكل زوج كلمات متجاورة، sideband_ω = |ω₁ − ω₂|
- `SpectralCouplingMatrix.fit()` → K_spectral[w₁][w₂] = |∫ Ψ₁*(ω) · Ψ₂(ω) dω|²، مع تخزين FFT cache + signal cache للأداء
- `adaptive_context_filter(ψ, ctx)` → bandwidth = median(|Δ|)، mask = 1/(1+(δ/bw)²) لعزل معنى السياق
- `relational_interference(target, ctx_remove, ctx_add)` → ψ_result = ψ_target − ψ_ctx₁ + ψ_ctx₂ (جبر علائقي)

## الابتكار 10: PhysicsGenerativeNetwork (PGN)

التوليد عبر البحث عن مسار الطاقة القصوى:
- بناء رسم بياني موزون للرنين الطيفي بين كل الكلمات
- `find_path(start_words, max_steps)` → أعلى مسار للطاقة من كلمات البداية
- أسرع من standard (0.015s vs 0.018s) بفضل ذاكرة التخزين المؤقت لـ FFT والإشارات

## الابتكار 11: التكافؤ الإنجليزي الكامل (V5.3)

النظام لم يعد مقتصراً على العربية. تم بناء بنية تحتية إنجليزية كاملة:

**EnglishMorphology:**
- POS tagging بقواعد لاحقات وأوزان (12 صنف: det, noun, verb, adj, adv, prep, pron, conj, aux, num, intj, part)
- تحليل صرفي: زمن (present/past/future)، عدد (singular/plural)، شخص (first/second/third)، نفي، بادئات
- EnglishStemmer لاستخراج الجذع (went→go, running→run, shines→shine, children→child)

**EnglishGrammarEngine:**
- كشف نوع الجملة (declarative/interrogative/imperative)
- توافق الفعل والفاعل (is/are, was/were, has/have, verb+s)
- تصحيح أدوات a/an

**EnglishWeightResonance:**
- 14 وزناً إنجليزياً: noun_sing, noun_pl, noun_proper, verb_present, verb_past, verb_gerund, verb_participle, adj_base, adj_comp, adj_sup, adv, det, prep, pron
- مصفوفة انتقالية 14×14 (det→noun 0.95, pron→verb 0.9, noun→verb 0.8, …)
- متجهات طورية 22D لكل وزن

**English Poetic Meters:**
- 6 أبحُر: iambic pentameter, iambic tetrameter, iambic trimeter, trochaic tetrameter, anapestic trimeter, dactylic dimeter
- syllable counter (معالجة silent e, -le, vowel clusters)
- rhyme groups (ee, ay, oo, ow, oy, ar, er, ate, ight)

**English Syntax Anchors:**
- noun=[0.2,1.0,0.1,0,0.3,0.1], verb=[1.0,0.2,0,0,0.15,0.1], adj=[0.1,0.5,0,1.0,0.1,0], adv=[0.1,0.1,0,0.3,1.0,0], det=[0,0.8,0.7,0,0,0], prep=[0,0.7,1.0,0,0.1,0], pron=[0.5,0.5,0.1,0,0.1,0], conj=[0.2,0.2,0.1,0.05,0.05,1.0]

---

## الابتكار 12: الذاكرة الطيفية العالمية (Global Spectral Memory — V5.4)

في V5.3، كان `align` يقارن المرشح فقط مع **آخر 3-5 كلمات** محلياً. المشكلة: الكلمة لا "تفهم" سياقها الكامل عبر الكوربوس. مثلاً "الملك" و"الملكة" لا يترابطان طيفياً رغم تقاربهما الدلالي.

**الآلية — التوقيع الطيفي العالمي (GSS):**
- لكل كلمة، نجمع **كل سياقاتها** عبر الكوربوس (نافذة 15 كلمة لكل ظهور)
- نستخدم **Coherence-Weighted Summation** بدلاً من المتوسط الحسابي:
  - نحسب المرجع (متوسط كل متجهات السياق)
  - نزن كل ظهور بـ `cos(θ)` مع المرجع (ReLU)
  - نجمع مرجحاً — هذا يعزز السياقات المتسقة ويُهمل الشواذ
- نطرح **Ground State** (متوسط كل سياقات الكوربوس) لعزل الإشارة النقية

**الفيزياء:**
```
ψ_signal = Normalize(Σ w_i · ψ_i) − ψ_ground    حيث w_i = max(cos(ψ_i, ψ_ref), 0)
```

**التأثير:** الرنين العالمي (`global_resonance`) يقيس توافق المرشح مع "جوهره" المخزّن عبر كل سياقاته في الكوربوس كله، وليس فقط آخر 5 كلمات.

## الابتكار 13: غرفة الرنين القصدي (Phase Opposition Chamber — V5.4)

كاشف التناقض المنطقي عبر التعاكس الطوري. إذا كانت زاوية الطور بين الكلمة المرشحة وكلمات السياق ~90°، يكون `|cos| ≈ 0`، مما يعني أن الكلمة في حالة **تعاكس دلالي**.

**الآلية:**
- تُطرح حالة الأرضية (Ground State) من كل من الكلمة ومتجهات السياق لعزل التناقض الحقيقي عن الضجيج
- يُحسب متوسط `|cos(ψ_word − ψ_ctx)|` عبر كل كلمات السياق
- العتبات:
  - `< 0.15`: تعاكس طوري → تناقض منطقي → score = −1.0
  - `> 0.70`: توافق طوري → score = +0.5
  - غير ذلك → محايد → score = 0.0

**المعادلة:**
```
ψ_clean = ψ − ψ_ground
θ = mean(|cos(ψ_word_clean − ψ_ctx_clean)|)
score = −1.0 if θ < 0.15 else +0.5 if θ > 0.70 else 0.0
```

## الابتكار 14: المعمارية الهرمية لـ 3 مصفوفات K (V5.4)

بدلاً من مصفوفة اقتران واحدة، أصبح النظام يستخدم 3 مصفوفات هرمية لمنع التشبع الطيفي عند ضخ البيانات:

**K_syn (نافذة 2):** تلتقط التراكيب النحوية القصيرة — حرف جر + اسم، فعل + فاعل.
**K_sem (نافذة 5):** المصفوفة الأصلية — الترابط السياقي المتوسط.
**K_conc (من GSS):** مصفوفة مفاهيمية تربط الكلمات المتشابهة دلالياً عبر `cos(gss_i, gss_j)` حتى لو لم تظهر معاً في نافذة واحدة.

**آلية الترجيح الديناميكي:**
- أول 3 كلمات من التوليد: `dyn_syn=1.5, dyn_sem=1.0, dyn_conc=0.5` — تغليب النحو
- بعد الكلمة الثالثة: `dyn_syn=0.5, dyn_sem=1.0, dyn_conc=1.5` — تغليب الدلالة والمفاهيم

كل درجة تُضرب بعامل `act = mean(exp(β · cos))` للتنشيط الطوري قبل الإضافة إلى النتيجة النهائية.

## الابتكار 15: محرك الاستدلال السببي (CausalPhaseEngine — V5.4)

يكشف العلاقات السببية من ترتيب الكلمات في الجمل. المبدأ: إذا كانت A تظهر قبل B باستمرار، وفرق الطور بينهما ~90°، فـ A تسبب B.

**الآلية:**
- `build_from_corpus()`: يبني مصفوفة `causal[i,j] = (before - after) / (before + after)` — تعكس مدى سبق i على j
- `causal_strength(i, j)`: درجة 0..1 أن i تسبب j
- `transitive_score(ids)`: للاستدلال المتعدد الخطوات — إذا A→B و B→C، فنتوقع A→C
- يُدمج في `_score()` كـ `causal_score`: يكافئ الكلمات التي تستمر في الاتجاه السببي

**المعادلة:**
```
causal[i,j] = (count(i قبل j) - count(j قبل i)) / (count(i قبل j) + count(j قبل i) + 1)
```

## الابتكار 16: السلسلة الرنانة LC (ResonantChain — V5.4)

كل كلمتين متجاورتين في الجملة تشكلان دائرة رنين LC:

- **المكثف الدلالي C(i,j)**: صفيحتان المكثف هما كتلتا الكلمتين. العازل بينهما هو التوافق الطوري `cos(pv_i, pv_j)`. السعة:
  ```
  C(i,j) = mass_i · mass_j · (1 + cos(pv_i, pv_j))
  ```
  كتلتان كبيرتان + توافق طوري عالٍ ← سعة كبيرة ← تردد رنين منخفض.

- **المحاثة الدلالية L(i,j)**: الاقتران بين الكلمتين. كلما كانت الأطوار متقاربة، زادت المحاثة:
  ```
  L(i,j) = 1 / (1 + |sin(Δφ/2)|)
  ```

- **تردد الرنين الطبيعي** للزوج:
  ```
  f_res(i,j) = 1 / (2π √(C·L))
  ```

- **تماسك الجملة**: كلما كانت ترددات الرنين لجميع الأزواج المتجاورة متقاربة، كانت الجملة أكثر تماسكاً (جملة كسلك معدني منتظم الذرات). نحسب:
  ```
  coherence = 1 - σ(f) / σ_max(f)
  ```
  حيث σ(f) هو الانحراف المعياري لترددات الرنين، وσ_max(f) هو أقصى انحراف ممكن.

- **تسجيل المرشح**: عند توليد كلمة جديدة، نحسب `f_res` للزوج (prev_word, candidate) ونقارنه بمتوسط ترددات الأزواج السابقة. كلما كان قريباً من المتوسط، زادت الدرجة — وهذا يضمن أن الجملة تنمو كسلك رنان واحد متماسك.

**الأثر:** يمنع القفزات الدلالية المفاجئة بين الكلمات المتجاورة. يجبر الجملة على أن تكون "سلكاً" واحداً متجانساً بدلاً من "قطع منفصلة".

**الإيقاف المبكر الرنيني:** أثناء التوليد، يحسب `sentence_coherence()` للجملة بعد كل كلمة. إذا انخفض التماسك تحت 0.3، تُضاف نقطة (.) ويُقطع — الجملة فقدت اتساقها الرنيني، فلا فائدة من الاستمرار.

---

## الابتكار 17: محرك البرمجة (CodeEngine — V5.4)

توليد كود Python بنفس الآلية الفيزيائية — أطوار، رنين، مصفوفة اقتران:

- `tokenize_code(source)`: 16 نوع token (KEYWORD, IDENTIFIER, OPERATOR, LITERAL_NUM, LITERAL_STR, BRACKET, INDENT, DEDENT, NEWLINE, COMMENT, DECORATOR, TYPE_ANNOT, ASSIGN, COMPARE, LOGICAL, BUILTIN).
- `validate_syntax(tokens)`: بوابة نحوية بمصفوفة VALID_NEXT (16×16) — تحدد ما هو legal بعد كل نوع token. مثال: `KEYWORD → [IDENTIFIER, BUILTIN, BRACKET, OPERATOR, NEWLINE]`.
- `compile_check(source)`: compile() حقيقي للثعبان + التقاط SyntaxError.
- `CodeVocabulary`: معجم برمجي (مثل Vocabulary اللغوي لكن للـ tokens).
- `build_K_code(corpus)`: نافذة 5، يبني مصفوفة اقتران K_code من كوربس Python.
- `CodePhaseVector`: متجه 64D لكل code token، بذر من قيمة التجزئة ونوع token.
- `CodeEngine.suggest_next(tokens)`: يستعلم K_code + VALID_NEXT ← قائمة tokens مقترحة.
- `CodeEngine.generate_python(prompt)`: 6 قوالب (function/loop/class/if/import/assign) + `_gen_resonant` للتوليد غير المقولب.
- `_code_generate(prompt)` في Generator: يُفعَّل بـ `mode='code'`، يبني CodeEngine مع K_code من corpus_texts.

**الأثر:** يحوّل المرنان من مولد نصوص إلى مولد كود. لا حاجة لـ LLM خارجي — البوابة النحوية تحجب 100% من الكود غير الصحيح نحوياً، وK_code يمثل توزيعاً احصائياً حقيقياً للـ tokens البرمجية.

---

## الابتكار 18: Machine Tafsīr — التفكيك الطيفي للمعنى (V5.4)

تحليل معنى الكلمة عبر 4 طبقات فيزيائية:

1. **طبقة الحروف:** كل حرف يُفكَّك إلى متجه 22D (`letter_db.py`) + operator (بنائي/تدميري/محايد) + معنى لغوي. يُظهر أي الأبعاد الـ22 هي المسيطرة.
2. **طبقة الجذر:** استخراج الجذر الصرفي (`_extract_root_light`) — 3 أحرف تحمل المعنى الأساسي.
3. **طبقة الطيف:** FFT على متجهات الحروف → ترددات مسيطرة. التردد العالي = معنى تجريدي، المنخفض = حسي/مادي.
4. **طبقة الكميات الفيزيائية:** الكتلة، الطاقة، التردد الذاتي، الطور — من دالة الموجة الكلية.

**الخرج:** شرح عربي طبيعي يقرأه الإنسان (`explain("السلام")`) + مصفوفة رنين بين كلمات العبارة (`tafsir("العلم نور")`).

---

## الابتكار 19: Rust Core — تسريع نواة الفيزياء (V5.4)

مكتبة `mirnan_core` مكتوبة بـ **Rust + PyO3**، توفر:

- `word_spectrum_fft(letter_vecs)`: FFT حقيقي على متجهات الحروف — نتائج مطابقة تماماً لـ Python.
- `kuramoto_step(theta, omega, K, dt)`: خطوة RK4 واحدة لمحاكاة كوراموتو — تسريع **12.9×** مقارنة بـ Python.
- `kuramoto_order(theta)`: معامل الترتيب R (مدى التزامن بين المتذبذبات).

**الأثر:** فتح الطريق لنقل الحسابات الثقيلة (بناء K_matrix، FFT لآلاف الكلمات) إلى Rust مع بقاء API بايثون.

---

## الابتكار 20: DCCF — Dynamic Contextual Coupling Field (V6)

**بديل فيزيائي لـ self-attention** — لا QKV ولا Softmax ولا تعلّم.

**الآلية:**
- لكل كلمتين في السياق: `coupling = phase_align × mass_sim × dist_decay`
- `phase_align` = توافق طوري بين متجهي الكلمتين (cosine similarity)
- `mass_sim` = حاصل ضرب الكتلتين (الكلمات الثقيلة = أدوار مهمة)
- `dist_decay` = `exp(-decay_rate × |i-j|)` — العلاقات القريبة أقوى
- `get_context_boost(word, ctx)` يعطي درجة اقتران (0..1) لكل مرشح
- **مدمج في `_score()`** بوزن `raw_dccf = 2.00`

**لماذا بديل self-attention:**
- لا مصفوفات QKV — لا تعلم
- لا O(n²) مطلق — فقط السياق الحالي (n < 15)
- لا softmax — ضرب فيزيائي مباشر
- الإيقاع (dist_decay) مضبوط فيزيائياً، ليس مكتسباً

---

## الابتكار 21: PPM — Prompt Phase Modulation (V6)

**محاكاة فيزيائية لـ In-Context Learning (Few-Shot).**

**الآلية:**
- `absorb(prompt)`: يحوّل الـ prompt إلى متجه طوري متوسط `field = mean(PVs) × strength`
- `score(w_pv)`: درجة توافق المرشح مع الحقل: `cos(w_pv, field)`
- `step()`: الحقل يضمحل بعد كل خطوة توليد: `field *= (1 - decay_rate)`
- `reset()`: إعادة تعيين عند طلب جديد

**لماذا يضمحل:**
- الأمثلة الأولى في الـ prompt تؤثر بقوة
- الأمثلة البعيدة تتلاشى — مثل ذاكرة قصيرة المدى فيزيائية

---

## الابتكار 22: AMFS — Adaptive Mass & Frequency Shift (V6)

**تعديل الكتلة والتردد الذاتي للكلمة حسب السياق — محاكاة contextual embeddings.**

**الآلية:**
- `centrality` = متوسط التوافق الطوري للكلمة مع كل كلمات السياق
- `adapted_mass = base_mass × (1 + 0.3 × max(0, centrality))`
- `phase_shift = cos(w_pv, mean(ctx_pvs))`
- `adapted_freq = base_freq × (1 + 0.15 × tanh(phase_shift))`

**الأثر:**
- اسم في سياق علمي → كتلة أكبر → جاذبية أقوى
- كلمة غامضة في سياق واضح → تردد مختلف → معنى محدّد

---

## الابتكار 23: PhysicsOrchestrator (V6)

**موحّد فيزيائي يدير كل المحركات ديناميكياً.**

**الوظائف:**
- `_detect_mode(prompt)`: يكتشف الوضع تلقائياً من الـ prompt
  - يحتوي على كلمات مثل "شعر" أو "قصيدة" → وضع `poetic`
  - يحتوي على علامات `+`, `-`, `=`, كلمات مثل "math", "calculate" → وضع `quantum`
  - يحتوي على `def `, `class `, `import` → وضع `code`
  - أكثر من 8 كلمات → وضع `multiverse`
  - وإلا → وضع `standard`
- `generate(prompt, mode='auto')`: توليد مع كشف تلقائي
- `get_report()`: تقرير فيزيائي شامل (entropy, beta, k_B, DCCF, PPM, AMFS)
- `adjust_beta(val)` / `adjust_k_B(val)`: ضبط حي للمعاملات الفيزيائية

**الحالة الفيزيائية (`PhysicsState`):**
```python
{
  'entropy': 0.23, 'S_crit': 0.5, 'k_B': 1.0, 'beta': 2.0,
  'phase_coherence': 0.81, 'mass_mean': 0.45, 'mass_std': 0.12,
  'dccf_coupling': 0.34, 'ppm_field': 0.21, 'amfs_centrality': 0.55,
  'mode': 'poetic', 'is_poetic': True, 'word_count': 7
}
```

---

## الابتكار 24: PhaseReinforcement (V6)

**تعلّم بدون backprop — تعزيز طوري ذاتي (يشبه Hebbian learning في فضاء طوري).**

**الآلية:**
- `reinforce(word, pv, reward)`: `reinforced = old + lr × reward × (pv - old)`
- `apply(word, pv)`: `result = (1 - α) × pv + α × reinforced` (حيث α = strength × 0.3)
- `weaken(word, pv, penalty)`: تخفيض قوة الأثر
- `reinforce_sentence(words, pvs)`: تعزيز جملة كاملة بعد توليد ناجح

**الأثر:**
- الكلمات التي استُعملت بنجاح تصبح أسهل في الاستدعاء
- الكلمات التي أدّت لتوليد ضعيف تضعف
- مع الوقت، تفضيلات الكلمات تتشكل فيزيائياً — مثل التعلم، لكن بلا خسارة

---

## الابتكار 25: PoeticGravityEngine — 16 بحراً عربياً (V6)

**إكمال كل البحور الشعرية العربية الـ 16.**

قبل V6: 3 أبحُر فقط (طويل، كامل، رجز).
بعد V6: 16 بحراً كاملة + كشف تلقائي + تقطيع عربي دقيق.

| الرقم | البحر | نمط التفعيلات |
|-------|-------|--------------|
| 1 | الطويل | فعولن مفاعيلن فعولن مفاعيلن |
| 2 | المديد | فاعلاتن فاعلن فاعلاتن |
| 3 | البسيط | مستفعلن فاعلن مستفعلن فاعلن |
| 4 | الوافر | مفاعلتن مفاعلتن فعولن |
| 5 | الكامل | متفاعلن متفاعلن متفاعلن |
| 6 | الهزج | مفاعيلن مفاعيلن |
| 7 | الرجز | مستفعلن مستفعلن مستفعلن |
| 8 | الرمل | فاعلاتن فاعلاتن فاعلن |
| 9 | السريع | مستفعلن مستفعلن فاعلن |
| 10 | المنسرح | مستفعلن مفعولات مستفعلن |
| 11 | الخفيف | فاعلاتن مستفعلن فاعلاتن |
| 12 | المضارع | مفاعيلن فاعلاتن |
| 13 | المقتضب | مفعولات مستفعلن |
| 14 | المجتث | مستفعلن فاعلاتن |
| 15 | المتقارب | فعولن فعولن فعولن فعولن |
| 16 | المتدارك | فاعلن فاعلن فاعلن فاعلن |

`detect_meter(words)` يكتشف البحر تلقائياً من أي نص عن طريق مطابقة النبض الإيقاعي مع كل البحور.

---

## الابتكار 26: Potential Cascade Layer (V6)

**تحول مفاهيمي: من التوقع الاحتمالي إلى الهوي الجهدي الحتمي.**

في كل أنظمة اللغة التقليدية (LLMs وحتى الأنظمة الفيزيائية السابقة في المرنان)، يتم اختيار الكلمة التالية عبر توزيع احتمالي — softmax يعطي احتمالات، temperature يتحكم بالعشوائية، sampling يختار عينة. PotentialCascadeLayer يلغي هذا تماماً.

**المبدأ:**
كل كلمة تُولَّد تخلق "بئر جهد" (potential well) في فضاء الطور 64D. الكلمة التالية لا تُختار عشوائياً — إنها **تهوي حتمياً** إلى أعمق نقطة في هذا الحقل التراكمي، مثل الماء يجد مجراه الطبيعي، أو كوكب يسقط في بئر جاذبية.

**المعادلة الأساسية:**

```
V_total(c) = -λ Σ_i [ m_i · max(0, cosΔφ_{i,c}) / (d_pos(i) + δ)^γ ]
```

| الرمز | المعنى | القيمة الافتراضية |
|-------|--------|-------------------|
| λ (lambda_cascade) | معامل شدة الطبقة | 1.8 |
| γ (gamma) | أس اضمحلال المسافة (تربيع عكسي مثل الجاذبية/كولوم) | 2.0 |
| δ (delta) | ثابت تجنب التفرد عند المسافة صفر | 0.3 |
| m_i | الكتلة الدلالية للكلمة i في السياق | — |
| cosΔφ_{i,c} | التوافق الطوري (الرنين الدلالي) بين المرشح والسياق | — |
| d_pos(i) | المسافة الموضعية (1 للكلمة السابقة مباشرة، 2 للتي قبلها…) | — |

**المكونات:**

1. **Phase Lock Gate:** المرشح يجب أن يحقق `cos(candidate, last_context) > 0.65` وإلا يُرفض فوراً (`-inf`). يضمن الاستمرارية الطورية مع آخر كلمة في السياق.

2. **Syntax Wall:** إذا كان المرشح مخالفاً نحوياً (مثل فعل بعد حرف جر في العربية)، يُعيد `-inf` فوراً. لا كلمة غير نحوية تعبر.

3. **Pauli Repulsion:** يمنع تكرار الكلمات القريبة حديثاً — مثل مبدأ باولي في فيزياء الكم (لا يمكن لإلكترونين أن يشغلا نفس الحالة الكمومية). تنافر عكسي مع مربع المسافة في فضاء الطور.

4. **Friction (Friction Decay):** كلما طالت الجملة، يزيد الاحتكاك مع المسار الحالي — يمنح فرصة للكلمات الجديدة ويحد من الجمود.

**التكامل:**
- في `Generator.__init__()`: يُنشئ `PotentialCascadeLayer(lambda_cascade=1.8, gamma=2.0, delta=0.3)`
- في `_score()`: `score += W['cascade'] * cascade_score` حيث `raw_cascade = 1.50`
- في `_init_weights()`: `W['cascade'] = raw_cascade * scale`
- التفعيل: `orch.set_cascade(True/False, lambda_cascade=1.8)` أو عبر API: `"cascade": true, "cascade_strength": 2.0`
- الحالة: `PhysicsState.cascade_enabled` و `PhysicsState.cascade_strength`

**الفرق الجوهري:**

| النظام التقليدي | PotentialCascadeLayer |
|----------------|----------------------|
| softmax → احتمالات | لا softmax |
| temperature يتحكم بالعشوائية | لا temperature |
| sampling يختار عينة | لا sampling |
| قد يختار كلمة ضعيفة إذا كان الحظ سيئاً | يختار حتماً أعمق بئر جهد |
| غير حتمي (نفس المُدخل قد يُنتج مخرجات مختلفة) | حتمي تام (نفس المُدخل = نفس المخرج) |
| التوليد = مقامرة إحصائية | التوليد = هبوط في حقل جهدي محسوب |

**الفيزياء:** هذه ليست مجرد دالة تسجيل جديدة — إنها نقلة من "التوقع الاحتمالي" إلى "السقوط الجهدي الحتمي". الكلمة التالية ليست مرجحة، إنها **مقررة** بفعل الجهد التراكمي لكل السياق السابق. مثل حجر يُلقى في واد — مساره تحدده الجاذبية وموقع الجدران، لا قرعة ولا عشوائية.

---

## الابتكار 27: DialogueEngine — محرك حوار يمنع الاستكمال الإحصائي (V6)

**تحول جوهري: من الاستكمال الإحصائي إلى الحوار القصدي.**

في كل أنظمة التوليد التقليدية (وحتى في المرنان قبل هذا الابتكار)، عندما يدخل المستخدم نصاً مثل "السلام عليكم"، يعامله النظام كبداية جملة ويبحث في مصفوفة K عن الكلمة التالية الأكثر احتمالاً — فيجد "والتسكين" لأن الكلمتين تترافقان إحصائياً في الكوربوس. هذه هي مشكلة **الاستكمال الإحصائي**: النظام يكمّل النص بدلاً من أن يرد عليه.

**الحل — DialogueEngine:**

محرك حوار يحوّل المولد من محرك استكمال إحصائي إلى وكيل محادثة. يكتشف أن المدخل هو تحية، يسترجع هدف الاستجابة من AssociativeMemory، ويُلقّح المولد ببادئة رد مناسبة (مثل "وعليكم") بدلاً من مواصلة نص المستخدم.

**المكونات:**

1. **التسجيل المضاد للاستكمال** في `compute_dialogue_score()`:
   - يُكافئ الكلمات التي تتوافق مع هدف الاستجابة (من AssociativeMemory)
   - يُعاقب الكلمات التي تكتفي بمواصلة نص المستخدم (cos > 0.7 مع آخر كلمة = عقوبة)
   - يُكافئ كلمات بدء الجمل (مثل إن، قد، هذا، the, this)
   - يُعاقب تكرار الكلمات المستخدمة حديثاً

2. **الاستهداف القصدي للاستجابة**: يستخدم IntentDetector (تحية، سؤال، أمر، …) لاسترجاع أنماط الرد المناسبة من AssociativeMemory

3. **الكشف التلقائي للحوار**: `detect_need_for_dialogue()` يكتشف إذا كان الإدخال محادثياً

4. **التوليد المُلقّح**: بدلاً من إكمال نص المستخدم، يجد أقرب كلمة في المفردات لهدف الاستجابة ويبدأ التوليد منها

5. **الإيقاف التلقائي**: يتوقف بعد أول جملة كاملة (نقطة، علامة استفهام، إلخ.)

**بذور الاستجابة حسب القصد:**

| القصد | بذور الرد |
|-------|----------|
| تحية (GREETING) | وعليكم, أهلا, مرحبا, الله, تحياتي, مرحباً |
| سؤال (QUESTION) | السؤال, الجواب, الأمر, بالنسبة, الموضوع, حسناً |
| أمر (COMMAND) | حسناً, بالتأكيد, سأفعل, فوراً, الآن |
| طلب (REQUEST) | بالطبع, تفضل, حاضر, بكل, يسرني |
| وداع (FAREWELL) | في, إلى, مع, وداعاً, سلام |
| تصريح (STATEMENT) | نعم, صحيح, بالفعل, تماماً, هذا |
| رأي (OPINION) | رأي, برأيي, أرى, أعتقد, وجهة |
| اقتراح (SUGGESTION) | اقتراح, فكرة, ممتاز, لنجرب, الأفضل |
| شكوى (COMPLAINT) | آسف, نأسف, سأحل, المشكلة, نعم |
| وعد (PROMISE) | أعدك, سألتزم, بكل, موعد, إن شاء |
| شكر (THANK) | العفو, لا شكر, أهلاً, سررت, خدمة |

**التكامل:**
- في `Generator.__init__()`: يُنشئ `self.dialogue_engine` و `self.dialogue_mode`
- في `_init_weights()`: يُضاف `raw_dialogue = 3.00` بمفتاح `'dialogue'`
- في `_score()`: يُضاف `dialogue_score` مرجّحاً بـ `W['dialogue']` عندما `self.dialogue_mode` مفعّل
- في `Generator.generate()`: معالجة `mode='dialogue'` عبر `_dialogue_generate()`
- في `PhysicsOrchestrator`: `set_dialogue(enabled)`، كشف تلقائي في `_detect_mode()`
- في `PhysicsState`: حقول `dialogue_mode`, `dialogue_intent`, `dialogue_confidence`
- API: معامل `"dialogue": true/false`
- CLI: علم `--dialogue`، أمر `/dialogue ON|OFF`
- UI: مفتاح تبديل لوضع DIALOGUE

**الفرق الجوهري:**

| الاستكمال الإحصائي | DialogueEngine |
|-------------------|----------------|
| input → يجد الكلمة التالية من K matrix | input → يكشف القصد → يسترجع قالب الرد → يُلقّح ببادئة رد |
| يكمّل نص المستخدم | يولّد جملة رد مستقلة |
| "السلام عليكم" → "والتسكين" (ترافق إحصائي) | "السلام عليكم" → "وعليكم السلام" (رد) |
| لا يعرف متى يتوقف | يتوقف عند أول حد جملة |
| إحصائي محض | قصدي، مدرك للسياق |

---

## ابتكارات V8: الرنين الجسيمي (Partonic Resonance)

### الابتكار 22: مصفوفة الكثافة الكمومية (Quantum Density Matrix)
**الملف:** `src/physics/density_matrix.py`

استبدال `np.average(pvs)` — الذي يدمج متجهات السياق إتلافياً ويفقد العلاقات البينية — بمصفوفة كثافة كمومية:
```
ρ = Σᵢ pᵢ |ψᵢ⟩⟨ψᵢ|    (مصفوفة 64×64)

رنين المرشح: R(v) = ⟨v| ρ |v⟩ = Σᵢ pᵢ · |⟨v|ψᵢ⟩|²
```
- كل كلمة سياقية تحتفظ بمساهمتها المنفردة عبر outer product
- الاضمحلال الموضعي `pᵢ ∝ exp(-0.8·i)` يعطي وزناً أكبر للكلمات الأقرب
- مؤشرات كمومية: `trace()` (طاقة)، `purity()` (نقاء)، `entanglement_entropy()` (تشابك)

**النتيجة:** لا ضياع للتفاصيل الدقيقة في السياقات الطويلة. إذا ذُكر اسم في أول الجملة، يبقى أثره في مصفوفة الكثافة.

### الابتكار 23: فصل الأطوار الجسيمي (Partonic Phase Separation)
**الملف:** مدمج في `generator.py` (`_partonic_resonance`)

معالجة المتجه 64-بُعداً كجسيم له نواة وغلاف:
```
النواة (Core):    dims 0:36  — حروف، جذر، تلبيس سياقي
الغلاف (Shell):   dims 42:64 — دلالة فلسفية، تداوليات

التسجيل الثلاثي:
  core_sim  = cosine(w[0:36], ctx[0:36])
  sem_sim   = cosine(w[42:64], ctx[42:64])
  cross     = 1 - |core_sim - sem_sim|
```

- **كلمات مثل "عَلِمَ" و"لَمَعَ":** core_sim مرتفع (نفس الحروف)، sem_sim منخفض (معنى مختلف) ← cross_coherence منخفض ← عقوبة
- الكلمة الجيدة: core_sim ≈ sem_sim ← cross_coherence ≈ 1.0 ← مكافأة

### الابتكار 24: حقل التدفق الطوري السببي (Causal Phase Flow)
**الملف:** `src/physics/causal_flow.py`

تحويل العلاقات السببية من مصفوفة متقطعة إلى حقل تدفق مستمر:
```
J(pv) = Σᵢ wᵢ · C_strength(i→j) · (pv_j - pv)

محاذاة التدفق: align = cos(v_cand - v_curr, J(v_curr))

استدلال تعددي: A→B→C: strength = Π cos(θ) · mag
```

- `causal_engine` **مُفعّل الآن** (وزن 2.0 في config، كان 0.0)
- التدفق يدفع المرشحات نحو مناطق أكثر اتساقاً سببياً
- يدعم استدلالاً متعدد الخطوات (transitive reasoning)

### الابتكار 25: التعديل الهرمي الموجي (Hierarchical Wave Modulation)
**الملف:** تكامل `hierarchical_memory.py` مع `spectral_wave_engine.py`

4 مستويات طيفية مع تعديل AM/FM متتالي:
```
L₁ (كلمات)    — تشابه طوري مباشر (AM سريع)
L₂ (عبارات)   — موجة حاملة (FM — تعديل التردد)
L₃ (فقرات)    — تعديل السعة (AM متوسط)
L₄ (محادثة)   — موجة الغلاف (Envelope — التعديل الأبطأ)

الدرجة = cos(core, L₁) · L₂ · L₃ · L₄
```

- ضغط تلقائي: 5 كلمات → عبارة، 4 عبارات → فقرة، 3 فقرات → محادثة
- اضمحلال كتلي متفاوت: 0.98 (كلمة) ← 0.85 (محادثة)
- **النتيجة:** يحاكي طبقات Transformer العميقة (نحوية ← دلالية ← سياقية ← تجريدية) دون شبكات عصبية

---

## الخطة المستقبلية V9+
1. **بصمة كاتب (Author Fingerprint)**: توقيع طوري فريد لكل كاتب.
2. **تداخل موسيقي (Music-Text Resonance)**: تحويل النص إلى MIDI.
3. **PhysicsTranslator واسع**: WordNet عربي-إنجليزي مع align طيفي كامل.
4. **فك الغموض الدلالي**: تفرع كمومي للسياقات المجازية.
5. **حوار طويل المدى**: PhaseReinforcement يتعلم من التقييم البشري.
6. **Rust Core شامل**: DCCF + FFT بالكامل في Rust.
